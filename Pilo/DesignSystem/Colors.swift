import SwiftUI

extension Color {
    static let piloBlue       = Color("PiloBlue")
    static let piloBlueLight  = Color("PiloBlueLight")
    static let piloBlueDark   = Color("PiloBlueDark")
    static let piloAccent     = Color("PiloAccent")   // 信封心粉，庆祝 / focus / bypass 强调条
    static let piloCream      = Color("PiloCream")    // 信封奶油，温暖 surface（about、success）

    // v3.3 邮局风：金色装饰 + 信纸黄 + 邮戳红绿
    static let piloGold       = Color("PiloGold")        // #D4A574 装饰金（线条、星、印章框）
    static let piloGoldDark   = Color("PiloGoldDark")    // #B8895A 金棕（斜体宋体标签）
    static let piloPaper      = Color("PiloPaper")       // #FFF8EB 信纸黄（commit/stash 卡片底）
    static let piloPaperBorder = Color("PiloPaperBorder") // #E5D9C4 信纸边线
    static let stampRed       = Color("StampRed")        // #B8553F 邮戳红（已沉睡等）
    static let stampMint      = Color("StampMint")       // #2D8B5A 邮戳绿（已寄出）

    static let mintSafe       = Color("MintSafe")
    static let amberWarn      = Color("AmberWarn")
    static let roseDanger     = Color("RoseDanger")
    static let lavenderInfo   = Color("LavenderInfo")

    static let creamBg        = Color("CreamBg")
    static let paperCard      = Color("PaperCard")
    static let cloudDivider   = Color("CloudDivider")

    static let inkPrimary     = Color("InkPrimary")
    static let inkSecondary   = Color("InkSecondary")
    static let inkTertiary    = Color("InkTertiary")
}
